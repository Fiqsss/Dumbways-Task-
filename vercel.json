{
    "version": 2,
    "builds": [
        {
            "src": "package.json",
            "use": "@frontity/now",
            "config": {"includeFiles": ["dist/**"]}
        }
    ],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "app.js"
        }
    ]
}
